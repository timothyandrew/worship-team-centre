<h2>This song list was done on <%= @song_list.done_on %></h2>
<%= link_to "Edit", edit_song_list_path(@song_list), class: "btn" %>

<%= render partial: "songs_in_list", locals: { song_list: @song_list } %>